

  // $(window).scroll(function(){ 
    // var height = $(document).scrollTop(); //실시간으로 스크롤의 높이를 측정
    // if(height > 1000){ 
      // $('.form_meun>ul').addClass('fixinner'); 
    // }else if(height == 0){ 
      // $('.form_meun>ul').removeClass('fixinner'); 
    // } 
  // });

